services:
  hugo:
    image: make-models-fair/tobefair:latest
    build:
      context: .
      args:
        HUGO_VERSION: "${HUGO_VERSION:-0.164.0}"
    ports:
      - "127.0.0.1:1313:1313"
    volumes:
      - .:/src
      - /src/node_modules
